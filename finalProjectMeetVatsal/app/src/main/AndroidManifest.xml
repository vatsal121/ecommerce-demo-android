<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mpvc.finalprojectmeetvatsal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectMeetVatsal">
        <activity android:name=".recycler.CartProductLayout"></activity>
        <activity android:name=".recycler.CartPage" />
        <activity android:name=".recycler.layout_products" />
        <activity android:name=".recycler.HomePage" />
        <activity android:name=".RegisterPage" android:theme="@style/Theme.AppCompat.NoActionBar"  />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>